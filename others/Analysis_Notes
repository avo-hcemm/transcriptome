				TranscriptomesAnalysis - Notes

				Sample:
1) MDAMB231_199_mapped.sorted.bam: human breast cancer cell line

				Data Preprocessing:
Mapping with minimap2 ?

				Workflow:
Load protein_coding.gtf
   ↓
Build IntervalTree per chromosome
   ↓
Open BAM with SamReader
   ↓
For each read:
    check overlap in interval tree
    if overlaps → analyze

1) only mapped reads
2) only primary alignments
3) mapping quality above 20 (PHRED)
3) Use indexed region queries to parallelize by chromosome	

Comment:
(06/03/206): reads may overlap not necessarily in regions of genes overlap.